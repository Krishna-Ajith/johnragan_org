
define( [ 'Backbone' ], function( Backbone )
{
	/*
	 * Defines the Tweet Model class which is used to encapsulate a single 
	 * Tweet instance. The Tweet Model simply serves as a wrapper around 
	 * the raw JSON object returned from the server which represents an 
	 * individual Tweet.
	 */
	var Tweet = Backbone.Model.extend({
	});
	return Tweet;
});
